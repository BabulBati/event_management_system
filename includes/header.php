<?php
/**
 * HEADER INCLUDE
 * ================
 * Reusable header component included at the top of every page.
 *
 * TODO:
 * - Start with <!DOCTYPE html>, <html>, <head> section
 * - Include <meta charset="UTF-8"> and viewport meta tag for responsiveness
 * - Set dynamic <title> tag (pass $pageTitle variable from each page)
 * - Link Bootstrap 5 CSS (CDN or local file in /assets/css/)
 * - Link custom stylesheet (/assets/css/style.css)
 * - Include any page-specific CSS if needed (pass $extraCSS variable)
 * - Open <body> tag
 * - Include navbar.php component
 *
 * NOTE:
 * - Start PHP session at the top if not already started (session_start())
 * - Include constants.php for BASE_URL and SITE_NAME
 * - This file should NOT close </body> or </html> — that's done in footer.php
 */
